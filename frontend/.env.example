# ── Copy this file to frontend/.env ──────────────────────────────────────────

# Direct URL to the MJPEG video stream (bypasses Vite proxy buffering)
VITE_STREAM_URL=http://localhost:8000/stream/video
